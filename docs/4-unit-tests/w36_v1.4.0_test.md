# Test Summary - Week 36, V. 1.4.0

## What Was Tested

- Adam wake-word, 30-second follow-up, reset, timeout, and standalone `donzo` state transitions.
- Bridge capability negotiation, follow-up audio envelopes, old-client defaults, request correlation, and malformed payload rejection.
- Authoritative STT interception so standalone `donzo` ends follow-up without invoking Hermes or TTS.
- Existing microphone conditioning, cue waveform, British male voice policy, and locale behavior.
- Release iOS compilation for the `AdamVoice` scheme.
- Vārti and class-level smart-home skill structure plus post-cache live loading.

## Test Results

- Total assertions/tests: 259
- Passed: 259
- Failed: 0
- Coverage: not collected; this repository uses focused standalone Swift and Python behavior suites.
- Release build: passed for generic iOS with code signing disabled.
- Hermes skill lint: 2 skills clean.

## Key Findings

- Bridges that do not advertise `follow_up_mode` retain the previous wake-only behavior.
- Only an exact punctuation/case/width/diacritic-tolerant `donzo` ends follow-up; longer phrases remain agent commands.
- Open and close wording for the pulse-mode Vārti relay intentionally maps to the same allowlisted `switch.turn_on` call.

## Notes

The physical gate was not activated during automated verification. A spoken owner acceptance test is required after installation because the relay has no independent position feedback.
