# Test Summary - Week 36, V1.4.1

## What Was Tested

- Private Hermes backend selection during first-use WebSocket authentication
- Streaming Kokoro TTS availability and PCM production in the configured Hermes environment
- Legacy LaunchAgent environment reproduction for the Edge fallback

## Test Results

- Total tests: 82
- Passed: 82
- Failed: 0
- Coverage: not collected for this targeted bridge fix

## Key Findings

- Token resolution can change the API base URL, so WebSocket URL construction must happen afterward.
- The always-on bridge used a legacy Hermes `venv` without `kokoro_mlx`; the current `.venv` produced 24 kHz PCM successfully.

## Notes

The iOS app is unchanged. The bridge and its private Hermes backend must be restarted after correcting the LaunchAgent environment.

The corrected first-use integration probe switched away from the protected dashboard, targeted the selected private backend, received a 24 kHz `start` frame, and produced 116,400 bytes of PCM. The same probe against the legacy `venv` returned `fallback`.
